import React, { useState, useEffect } from 'react'import { MdWifi, MdVolumeUp, MdBatteryFull, MdSearch, MdControlCenter } from 'react-icons/md'export default function MenuBar({ onAppleClick, onSearchClick, onControlCenterClick }) {  const [time, setTime] = useState(new Date())  useEffect(() => {    const timer = setInterval(() => setTime(new Date()), 1000)    return () => clearInterval(timer)  }, [])  const formatTime = (date) => {    return date.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' })  }  const formatDate = (date) => {    return date.toLocaleDateString([], { weekday: 'short', month: 'short', day: 'numeric' })  }  return (    <div className="menubar">      <div className="menubar-left">        <button className="menubar-btn apple-icon" onClick={onAppleClick}>                  </button>        <span className="menubar-app-name">Finder</span>        <span className="menubar-item">File</span>        <span className="menubar-item">Edit</span>        <span className="menubar-item">View</span>        <span className="menubar-item">Go</span>        <span className="menubar-item">Window</span>        <span className="menubar-item">Help</span>      </div>      <div className="menubar-right">        <button className="menubar-btn" onClick={onSearchClick}>          <MdSearch size={16} />        </button>        <div className="menubar-status">          <MdWifi size={16} />          <MdBatteryFull size={16} />          <MdVolumeUp size={16} />        </div>        <button className="menubar-btn clock" onClick={onControlCenterClick}>          <span className="menubar-date">{formatDate(time)}</span>          <span className="menubar-time">{formatTime(time)}</span>        </button>      </div>    </div>  )}